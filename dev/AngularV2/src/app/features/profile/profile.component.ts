import { Component } from '@angular/core';

@Component({
  selector: 'app-profile',
  template: `
    <h2>Profile (lazy loaded)</h2>
    <p>This module is lazy-loaded. Replace with real profile UI.</p>
  `
})
export class ProfileComponent {}
